def retr(url)
	retrieves a file via FTP, returns a file-like object

def unzip(file)
	unzips a file, returns a file-like object

def contract(file)
def contract_procedure(file)
def contract_cancel(file)
	parses XML, returns a tuple ready for inserting into db

--- update1 ---
1. Переписать insert на метод COPY вместо построчного, либо другой более эффективный.

--- update2 ---
!- Добавить индекс по publish_date, в таблицу notifications.
!- Убрать цикл пробегания по файлам в insert_notifications(files=[], db_con, ftp_con, namespace, region)
!- Добавить в таблицу notifications колонку max_price с ценой – суммой максимальной стоимости лотов.
!- Передвинуть работу с ошибками внутрь функции insert_notifications.

--- update3--- 
- Добавить обновляемый справочник продуктов.
- Переписать парсинг на iterparse.
!- Добавить запуск с помощью аргументов командной строки.
- Добавить в базу таблицу lots со списком лотов для каждой записи (id PK). Добавить индекс по id.