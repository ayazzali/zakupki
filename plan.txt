def retr(url)
	retrieves a file via FTP, returns a file-like object

def unzip(file)
	unzips a file, returns a file-like object

def contract(file)
def contract_procedure(file)
def contract_cancel(file)
	parses XML, returns a tuple ready for inserting into db

--- update1 ---
1. Добавить индекс по publish_date, folder_name в таблицу notifications.
2. Переписать insert на метод COPY вместо построчного, либо другой более эффективный.
3. Добавить в таблицу notifications колонку max_price с ценой – суммой максимальной стоимости лотов.
4. Добавить в базу таблицу lots со списком лотов для каждой записи (id PK). Добавить индекс по id.